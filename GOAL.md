# 🎯 长期目标：原生视频消息发送（逆向工程）

## 目标陈述

为 WeChat-Hook（aixed，微信 4.1.10.27）插件实现**原生视频消息发送**能力：
机器人将本地 mp4 以微信**原生视频消息**（type 43，群聊内联可播放卡片）发送到指定群聊，
形态与真人手机发视频完全一致。

## 可验收标准（按优先级）

1. **主验收**：测试群里出现原生视频消息（大号手机端显示为可直接内联播放的视频，
   **非文件图标**）
2. **技术验收**：hook HTTP API 新增 `POST /SendVideoMsg {wxidorgid, path}`，
   调用后微信以原生视频消息发送
3. **回退验收**：发送失败时机器人回复兜底文案（不静默）

## 已知事实（今晚实测）

- ✅ 已工作：SessionTable 轮询接收、SendTextMsg 发文本、SendImgMsg 发图片、
  SendImgMsg+filetype=43 发 **mp4 文件消息**（51MB 实测进群）
- ❌ 未解决：原生视频卡片（需要微信内部的 CDN 视频上传 + 视频消息构造）
- ❌ 已排除：UIA 自动化（4.x 无障碍树被剥空，实测两版本）、PostMessage 键盘（Qt 不处理）、
  3.9 客户端（新号登录被拒）
- 🔑 线索：aixed 源码 `Offsets::VIDEO_FIELD_VTABLE = 0x750C7F8 / VIDEO_FIELD_VTABLE2 = 0x750C888`
  ——作者已定位视频消息对象的虚表；send_message (0x1677A30) 是**通用消息分发器**
  （文本/图片都用它发送，视频结构若能正确构造，同一个函数应该也能发）

## 阶段计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| P0 情报 | aixed git 历史挖掘（被删代码）+ 社区 4.1.10.27 发视频方案搜索 | 找到可参考的接收/视频实现 |
| P1 动态采样 | hook send_message 倾倒用户 UI 发视频时的真实结构 | 获得原生视频消息的完整内存布局 |
| P2 构造 | 按 P1 布局实现 SendVideoMsg 构造器 | filehelper 收到原生视频消息 |
| P3 产品化 | HTTP 路由 + 桥接接入 + 测试 | 群里端到端验收 |

## 回退方案

若 P2 失败（视频结构过于复杂）：交付「缩略图 + 文件消息」混合形态
（OpenCV 抽首帧 + SendImgMsg 图片 + 文件消息），并保留 pywxrobot4 插件接入后门。
